# OpenAI tunnel only needs loopback. Use 0.0.0.0 only when LAN/VPN access is intentional.
HOST_SANDBOX_ROUTER_BIND=127.0.0.1
HOST_SANDBOX_ROUTER_PORT=8766
# HOST_SANDBOX_ROUTER_TOKEN=replace-with-a-long-random-secret
